# Docker Hub credentials for `make dockerhub-description` (pushes the repo overview).
# Copy to .release.env (gitignored) and fill in. Account-scoped PAT — does NOT
# touch your `docker login` session or any other Docker Hub account.
DOCKERHUB_USER=hedgiemate
DOCKERHUB_TOKEN=dckr_pat_xxx
